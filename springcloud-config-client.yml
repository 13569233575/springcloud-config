spring:
   profiles:
      active:
        - dev
---
server:
  port: 8201

spring:
  profiles: dev
  application:
      name: springcloud-config-test-client

eureka:
 client:
  service-url:
    defaultZone: http://admin:zhzy@eureka1:7001/eureka
---
server:
  port: 8102

spring:
  profiles: beta
  application:
    name: springcloud-config-test-client

eureka:
 client:
  service-url:
    defaultZone: http://admin:zhzy@eureka1:7001/eureka